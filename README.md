# TalkieTxt

Local-first macOS dictation app built with Tauri, Rust, `cpal`, and `whisper-rs`.

Project layout:

- `app/`: Tauri desktop app source
- `docs/`: product, architecture, UX, and troubleshooting notes

Start with [`app/README.md`](app/README.md) for local build and run instructions.
